{
  "name": "reactive",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "exports": "./src/index.js",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "test:cov": "node --test --experimental-test-coverage \"test/**/*.test.js\"",
    "test:mut": "npx -y -p @stryker-mutator/core@9.6.1 -p @stryker-mutator/tap-runner@9.6.1 stryker run --testRunner tap --testFiles \"test/**/*.test.js\" --reporters clear-text,progress --cleanTempDir always"
  }
}
